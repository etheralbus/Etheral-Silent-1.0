--[[
    COMPLETE BODYGYRO AIMLOCK WITH SLIDER HUD
    - X to toggle aimlock
    - F2/F3 to adjust FOV (slider HUD appears)
    - Rotating FOV circle follows mouse
    - Uses BodyGyro (no camera lock, bypasses anti-cheat)
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local TweenService = game:GetService("TweenService")

local Player = Players.LocalPlayer
local Mouse = Player:GetMouse()

-- CONFIG
local CONFIG = {
    FOVRadius = 90,
    MinFOV = 20,
    MaxFOV = 200,
    MaxDistance = 200,
    LockStrength = 0.25,
    MaxTorque = 600,
    FOVColor = Color3.fromRGB(220, 160, 255),
    DashLength = 10,
    GapLength = 12,
    RotationSpeed = 180,
    HUDAutoHideDelay = 5,
}

local isActive = false
local currentTarget = nil
local gyro = nil
local torso = nil

-- Wait for PlayerGui
local PlayerGui = Player:WaitForChild("PlayerGui", 5)
if not PlayerGui then return end

-- ===== CREATE GUI =====
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SilentAimFOV"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.Parent = PlayerGui

-- ===== SLIDER HUD =====
local hudFrame = Instance.new("Frame")
hudFrame.Name = "HUD"
hudFrame.AnchorPoint = Vector2.new(0.5, 0.5)
hudFrame.Position = UDim2.new(0.5, 0, 0.82, 0)
hudFrame.Size = UDim2.new(0, 650, 0, 90)
hudFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
hudFrame.BorderSizePixel = 4
hudFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
hudFrame.Visible = false
hudFrame.Parent = screenGui

local stroke = Instance.new("UIStroke")
stroke.Parent = hudFrame
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(120, 120, 120)

local accent = Instance.new("Frame")
accent.Parent = hudFrame
accent.Position = UDim2.new(0, 12, 0, 12)
accent.Size = UDim2.new(0, 28, 1, -24)
accent.BackgroundColor3 = Color3.fromRGB(145, 75, 255)
accent.BorderSizePixel = 3
accent.BorderColor3 = Color3.fromRGB(0, 0, 0)

local label = Instance.new("TextLabel")
label.Parent = hudFrame
label.BackgroundTransparency = 1
label.Position = UDim2.new(0, 70, 0, 0)
label.Size = UDim2.new(0, 120, 1, 0)
label.Text = "FOV"
label.Font = Enum.Font.Arcade
label.TextScaled = true
label.TextColor3 = Color3.fromRGB(240, 240, 240)
label.TextStrokeTransparency = 0
label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)

local sliderBG = Instance.new("Frame")
sliderBG.Parent = hudFrame
sliderBG.Position = UDim2.new(0, 210, 0.5, -11)
sliderBG.Size = UDim2.new(0, 320, 0, 22)
sliderBG.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
sliderBG.BorderSizePixel = 3
sliderBG.BorderColor3 = Color3.fromRGB(0, 0, 0)

local fill = Instance.new("Frame")
fill.Parent = sliderBG
fill.Size = UDim2.new(0.48, 0, 1, 0)
fill.BackgroundColor3 = Color3.fromRGB(150, 85, 255)
fill.BorderSizePixel = 0

local knob = Instance.new("Frame")
knob.Parent = sliderBG
knob.AnchorPoint = Vector2.new(0.5, 0.5)
knob.Position = UDim2.new(0.48, 0, 0.5, 0)
knob.Size = UDim2.new(0, 24, 0, 36)
knob.BackgroundColor3 = Color3.fromRGB(155, 90, 255)
knob.BorderSizePixel = 3
knob.BorderColor3 = Color3.fromRGB(0, 0, 0)

local valueBox = Instance.new("Frame")
valueBox.Parent = hudFrame
valueBox.Position = UDim2.new(1, -100, 0.5, -25)
valueBox.Size = UDim2.new(0, 75, 0, 50)
valueBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
valueBox.BorderSizePixel = 3
valueBox.BorderColor3 = Color3.fromRGB(150, 85, 255)

local number = Instance.new("TextLabel")
number.Parent = valueBox
number.BackgroundTransparency = 1
number.Size = UDim2.fromScale(1, 1)
number.Font = Enum.Font.Arcade
number.TextScaled = true
number.Text = "90"
number.TextColor3 = Color3.fromRGB(150, 85, 255)
number.TextStrokeTransparency = 0
number.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)

-- ===== FOV CIRCLE =====
local fovFrame = Instance.new("Frame")
fovFrame.Name = "FOVFrame"
fovFrame.Size = UDim2.new(0, CONFIG.FOVRadius * 2, 0, CONFIG.FOVRadius * 2)
fovFrame.Position = UDim2.new(0, 0, 0, 0)
fovFrame.BackgroundTransparency = 1
fovFrame.ClipsDescendants = true
fovFrame.Parent = screenGui

local circleContainer = Instance.new("Frame")
circleContainer.Name = "CircleContainer"
circleContainer.Size = UDim2.new(1, 0, 1, 0)
circleContainer.Position = UDim2.new(0, 0, 0, 0)
circleContainer.BackgroundTransparency = 1
circleContainer.Parent = fovFrame

local dashParts = {}
local function BuildDashes()
    for _, child in pairs(circleContainer:GetChildren()) do child:Destroy() end
    dashParts = {}
    local numDashes = math.floor((2 * math.pi * CONFIG.FOVRadius) / (CONFIG.DashLength + CONFIG.GapLength))
    local angleStep = (2 * math.pi) / numDashes
    for i = 1, numDashes do
        local angle = i * angleStep
        local x = CONFIG.FOVRadius * math.cos(angle)
        local y = CONFIG.FOVRadius * math.sin(angle)
        local dash = Instance.new("Frame")
        dash.Size = UDim2.new(0, CONFIG.DashLength, 0, 2)
        dash.Position = UDim2.new(0.5, x - CONFIG.DashLength/2, 0.5, y - 1)
        dash.BackgroundColor3 = CONFIG.FOVColor
        dash.BackgroundTransparency = 0
        dash.BorderSizePixel = 0
        dash.Rotation = math.deg(angle)
        dash.Parent = circleContainer
        table.insert(dashParts, { Frame = dash, Angle = angle })
    end
end
BuildDashes()

-- ===== STATUS LABEL =====
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(0, 280, 0, 25)
statusLabel.Position = UDim2.new(0.5, -140, 1.2, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "○ READY | FOV: " .. CONFIG.FOVRadius
statusLabel.TextColor3 = Color3.fromRGB(180, 120, 210)
statusLabel.TextSize = 13
statusLabel.Font = Enum.Font.SourceSansBold
statusLabel.Parent = fovFrame

local function UpdateStatus(text)
    statusLabel.Text = text .. " | FOV: " .. CONFIG.FOVRadius
end

-- ===== HUD FUNCTIONS =====
local function UpdateSlider(percent)
    percent = math.clamp(percent, 0, 1)
    local newFOV = math.round(CONFIG.MinFOV + (CONFIG.MaxFOV - CONFIG.MinFOV) * percent)
    newFOV = math.clamp(newFOV, CONFIG.MinFOV, CONFIG.MaxFOV)
    if newFOV ~= CONFIG.FOVRadius then
        CONFIG.FOVRadius = newFOV
        fovFrame.Size = UDim2.new(0, CONFIG.FOVRadius * 2, 0, CONFIG.FOVRadius * 2)
        BuildDashes()
        UpdateStatus(statusLabel.Text:match("^[^|]+"))
    end
    fill.Size = UDim2.new(percent, 0, 1, 0)
    knob.Position = UDim2.new(percent, 0, 0.5, 0)
    number.Text = tostring(CONFIG.FOVRadius)
end

local function ShowHUD()
    if hudHideTimer then task.cancel(hudHideTimer); hudHideTimer = nil end
    if not hudVisible then
        hudFrame.Visible = true
        hudFrame.Position = UDim2.new(0.5, 0, -0.12, 0)
        local rollDown = TweenService:Create(hudFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(0.5, 0, 0.82, 0)
        })
        rollDown:Play()
        hudVisible = true
    end
    local percent = (CONFIG.FOVRadius - CONFIG.MinFOV) / (CONFIG.MaxFOV - CONFIG.MinFOV)
    fill.Size = UDim2.new(percent, 0, 1, 0)
    knob.Position = UDim2.new(percent, 0, 0.5, 0)
    number.Text = tostring(CONFIG.FOVRadius)
    hudHideTimer = task.delay(CONFIG.HUDAutoHideDelay, function()
        local rollUp = TweenService:Create(hudFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(0.5, 0, -0.12, 0)
        })
        rollUp:Play()
        hudVisible = false
        task.wait(0.35)
        hudFrame.Visible = false
        hudHideTimer = nil
    end)
end

local hudVisible = false
local hudHideTimer = nil

-- ===== SLIDER DRAGGING =====
local dragging = false
sliderBG.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        local percent = (input.Position.X - sliderBG.AbsolutePosition.X) / sliderBG.AbsoluteSize.X
        UpdateSlider(percent)
    end
end)
sliderBG.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)
sliderBG.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local percent = (input.Position.X - sliderBG.AbsolutePosition.X) / sliderBG.AbsoluteSize.X
        UpdateSlider(percent)
    end
end)
knob.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true end
end)
knob.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)
knob.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local percent = (input.Position.X - sliderBG.AbsolutePosition.X) / sliderBG.AbsoluteSize.X
        UpdateSlider(percent)
    end
end)

-- ===== BODYGYRO =====
local function SetupGyro()
    local char = Player.Character
    if not char then return false end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    if gyro then gyro:Destroy() end
    torso = hrp
    gyro = Instance.new("BodyGyro")
    gyro.Parent = torso
    gyro.MaxTorque = Vector3.new(CONFIG.MaxTorque, CONFIG.MaxTorque, CONFIG.MaxTorque)
    gyro.D = 20
    gyro.P = 2000
    gyro.CFrame = torso.CFrame
    return true
end

Player.CharacterAdded:Connect(function()
    task.wait(0.5)
    SetupGyro()
end)

if Player.Character then
    SetupGyro()
end

-- ===== TARGET FUNCTIONS =====
local function IsDowned(character)
    if not character then return true end
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid or humanoid.Health <= 0 then return true end
    local state = humanoid:GetState()
    return state == Enum.HumanoidStateType.Physics or
           state == Enum.HumanoidStateType.Dead or
           state == Enum.HumanoidStateType.FallingDown or
           state == Enum.HumanoidStateType.GettingUp
end

local function GetTargets(mousePos)
    local targets = {}
    local camPos = Camera.CFrame.Position
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= Player and player.Character then
            if IsDowned(player.Character) then continue end
            local head = player.Character:FindFirstChild("Head")
            if head then
                local headPos, onScreen = Camera:WorldToScreenPoint(head.Position)
                if onScreen then
                    local distance = (head.Position - camPos).Magnitude
                    if distance <= CONFIG.MaxDistance then
                        table.insert(targets, {
                            Player = player,
                            Head = head,
                            HeadScreenPos = Vector2.new(headPos.X, headPos.Y),
                            Distance = distance,
                            HeadPosition = head.Position,
                        })
                    end
                end
            end
        end
    end
    return targets
end

local function GetBestTarget(targets, mousePos, current)
    local best, bestDist = nil, CONFIG.FOVRadius + 1
    for _, t in pairs(targets) do
        local d = (t.HeadScreenPos - mousePos).Magnitude
        if d <= CONFIG.FOVRadius and d < bestDist then
            bestDist = d
            best = t
        end
    end
    if best and current then
        local currentDist = (current.HeadScreenPos - mousePos).Magnitude
        if bestDist < currentDist - 5 then
            return best
        else
            return current
        end
    end
    return best
end

-- ===== KEYBINDS =====
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.X then
        isActive = not isActive
        UpdateStatus(isActive and "● AIMING" or "○ READY")
        if not isActive then currentTarget = nil
    end

    if input.KeyCode == Enum.KeyCode.F3 then
        local newSize = math.min(CONFIG.FOVRadius + 10, CONFIG.MaxFOV)
        if newSize ~= CONFIG.FOVRadius then
            CONFIG.FOVRadius = newSize
            fovFrame.Size = UDim2.new(0, CONFIG.FOVRadius * 2, 0, CONFIG.FOVRadius * 2)
            BuildDashes()
            UpdateStatus(statusLabel.Text:match("^[^|]+"))
            ShowHUD()
        end
    end

    if input.KeyCode == Enum.KeyCode.F2 then
        local newSize = math.max(CONFIG.FOVRadius - 10, CONFIG.MinFOV)
        if newSize ~= CONFIG.FOVRadius then
            CONFIG.FOVRadius = newSize
            fovFrame.Size = UDim2.new(0, CONFIG.FOVRadius * 2, 0, CONFIG.FOVRadius * 2)
            BuildDashes()
            UpdateStatus(statusLabel.Text:match("^[^|]+"))
            ShowHUD()
        end
    end
end)

-- ===== ROTATION ANIMATION =====
local rotation = 0
RunService.Heartbeat:Connect(function(deltaTime)
    rotation = (rotation + CONFIG.RotationSpeed * deltaTime) % 360
    local angleOffset = math.rad(rotation)
    for _, data in pairs(dashParts) do
        local angle = data.Angle + angleOffset
        local x = CONFIG.FOVRadius * math.cos(angle)
        local y = CONFIG.FOVRadius * math.sin(angle)
        data.Frame.Position = UDim2.new(0.5, x - CONFIG.DashLength/2, 0.5, y - 1)
        data.Frame.Rotation = math.deg(angle)
    end
end)

-- ===== MAIN LOOP =====
RunService.RenderStepped:Connect(function()
    local mouseX, mouseY
    if Mouse then
        mouseX = Mouse.X
        mouseY = Mouse.Y
    else
        local pos = UserInputService:GetMouseLocation()
        mouseX = pos.X
        mouseY = pos.Y
    end

    if fovFrame then
        fovFrame.Position = UDim2.new(0, mouseX - CONFIG.FOVRadius, 0, mouseY - CONFIG.FOVRadius)
    end

    if not isActive or not gyro or not torso then
        if isActive and not gyro then SetupGyro() end
        return
    end

    local targets = GetTargets(Vector2.new(mouseX, mouseY))
    if #targets == 0 then
        if currentTarget then currentTarget = nil; UpdateStatus("○ NO PLAYERS") end
        return
    end

    local target = GetBestTarget(targets, Vector2.new(mouseX, mouseY), currentTarget)
    if target then
        if not currentTarget or target.Player ~= currentTarget.Player then
            currentTarget = target
        end
        local headPos = currentTarget.HeadPosition
        local lookAt = CFrame.lookAt(torso.Position, headPos)
        gyro.CFrame = gyro.CFrame:Lerp(lookAt, CONFIG.LockStrength)
        UpdateStatus("● " .. currentTarget.Player.Name .. " (" .. math.floor(currentTarget.Distance) .. "s)")
    else
        if currentTarget then currentTarget = nil; UpdateStatus("○ NO TARGET") end
    end
end)

Player.CharacterAdded:Connect(function()
    wait(0.5)
    if fovFrame then fovFrame.Visible = true end
end)

print("Aimlock loaded – X to toggle, F2/F3 for FOV")
